# remove Sg nom forms of some words that don't actually have them
#
define words [
        [ {iseenese} "+Pron"] |
        [ {ligida} "+A" ] |
        [ {läheda} "+A" ] |
        [ {mõlema} "+Pron" ] |
        [ {üksteise} "+Pron" ] |
        [ {teineteise} "+Pron" ] |
        [ {lühida} "+A" ] ] ;
define SGNOM [ "+Sg" "+Nom" ] ;

# make sure only word-final components are considered
define WB [ "#" ] ;

define lexicalw [ words SGNOM [\WB]* ] ;

regex ~[lexicalw] ;
